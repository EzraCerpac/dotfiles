#!/usr/bin/env bash
set -euo pipefail

APP_PATH="/Applications/PyCharm.app"
PYCHARM_BIN="${APP_PATH}/Contents/MacOS/pycharm"

if [[ ! -x "${PYCHARM_BIN}" ]]; then
    echo "PyCharm binary not found at ${PYCHARM_BIN}" >&2
    exit 1
fi

exec "${PYCHARM_BIN}" "$@"
