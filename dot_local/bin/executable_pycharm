#!/usr/bin/env bash
set -euo pipefail

APP_PATH="/Applications/PyCharm.app"

if [[ ! -d "${APP_PATH}" ]]; then
    echo "PyCharm.app not found at ${APP_PATH}" >&2
    exit 1
fi

exec open -na "${APP_PATH}" --args "$@"
