#!/usr/bin/env bash

set -euo pipefail

if [[ $# -eq 0 ]]; then
    echo "usage: bi <formula...>" >&2
    exit 2
fi

pending="${HOMEBREW_BUNDLE_PENDING:-$HOME/.local/state/chezmoi/brew-pending/Brewfile}"
mkdir -p "$(dirname "$pending")"
touch "$pending"

brew bundle add --install --no-describe --file "$pending" "$@"
echo "pending: $pending"
