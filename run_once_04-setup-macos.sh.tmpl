#!/usr/bin/env bash

set -euo pipefail

{{- if eq .chezmoi.os "darwin" }}

# macOS-specific system configuration. Casks live in run_onchange_03-install-packages.sh.tmpl.

echo "Setting up macOS defaults..."

echo "Configuring Dock to show only running apps..."
defaults write com.apple.dock static-only -bool true

echo "Configuring Mission Control to group windows by application..."
defaults write com.apple.dock expose-group-apps -bool true
killall Dock >/dev/null 2>&1 || true

echo "Configuring screenshot format to JPEG..."
defaults write com.apple.screencapture type -string jpg
killall SystemUIServer >/dev/null 2>&1 || true

echo "Configuring function keys to send standard F1-F12..."
defaults write -g com.apple.keyboard.fnState -bool true

echo "Enabling Mission Control Show Desktop shortcut..."
/usr/libexec/PlistBuddy -c "Set :AppleSymbolicHotKeys:36:enabled true" "${HOME}/Library/Preferences/com.apple.symbolichotkeys.plist" 2>/dev/null || true
killall cfprefsd >/dev/null 2>&1 || true
killall Dock >/dev/null 2>&1 || true
killall SystemUIServer >/dev/null 2>&1 || true

echo "macOS setup complete"

{{- end }}
