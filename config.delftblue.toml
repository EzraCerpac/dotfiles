# Retired: keep this guard so the old selector cannot silently apply the base.
# Previous cluster definitions remain in repository history.
[env]
DOTS_RETIRED_PROFILE = "{{ throw(message='The delftblue profile is retired. Choose workstation or nas only on supported machines; the cluster has not been migrated.') }}"
