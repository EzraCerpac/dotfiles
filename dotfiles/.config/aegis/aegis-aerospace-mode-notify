#!/usr/bin/env bash
set -euo pipefail

aerospace_bin=${AEROSPACE_BIN:-/opt/homebrew/bin/aerospace}
mode=$("$aerospace_bin" list-modes --current 2>/dev/null) || exit 0
exec "$HOME/.config/aegis/aegis-aerospace-notify" "mode:${mode:-main}"
